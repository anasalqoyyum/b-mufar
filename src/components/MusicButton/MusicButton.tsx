import { Button } from 'native-base'
import { styled } from 'nativewind'
import React from 'react'

const TButton = styled(Button)

export const MusicButton = () => {
  return <TButton>Music</TButton>
}
